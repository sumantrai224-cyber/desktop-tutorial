#include <stdio.h>
#include <math.h>

int main() {
    int a, b, c;

    printf("Enter a number: ");
    scanf("%d", &a);

    printf("Enter the other number: ");
    scanf("%d", &b);

    c = a + b;

    printf("Lawandaar: %d\n", c);

    return 0;
}
